[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Redeploy trigger: updated at 2026-07-15 to prompt Netlify to rebuild after dependency change
